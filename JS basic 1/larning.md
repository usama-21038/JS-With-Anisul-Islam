-Introduction to JS
    -What is JS?
    -What are the ways to add js?
    -Setting up the environment


    -user input:const number=parseInt(prompt("Enter your favorite number:"));
    const number=parsefloat(prompt("Enter your favorite number:"));
    const number=Number(prompt("Enter your favorite number:"));
    =>number received in string format

    =>number to string conversion
    
     toString(25);
     or
     lat number=12;
     number.toString();
     //aie number ta string e convert hoye jabe


    -output: alert(),confirm(),console
            console
                console.log("This is a message in the console.");
                console.warn("This is a warning message.");
                console.error("This is an error message.");
                console.info("This is some information.");
                console.clear();
    -comment: single line comment, multiple line comment   
    -Tokens: 
        -keyword 
        -puncuators
        -backslash characters
            characters=> 
                \n,\t, variables,data types,string,truthy vs falsy values,operatiors,
        -variables:var vs let vs const
        -datatype (typeof())=> number,string,boolean,null,undefined   
        -truthy vs falsy values 
        -operator :
            -Arithmetic(+,-,*,/,%)
                a+=b; //a=a+b;
                a+=1; //a=a+1;
            -Assignment
            -Relational or Coparision(>,<,<=,>=,==,!==,===, check data type)
    -control statement
    -conditional control statement: if, else,switch
    -loop control statement
    -Array,string , object

## JavaScript part-4 (Basic)
    -function vs method
        -function (traditional vs arrow)
        -Method- Number
                    -console.log(number.toFixed(2))
                    -console.log(number.toPrecision(3));
                    -console.log( number.toString());
                    -parseInt("12.56");
                    -parseFloat("12.56");
                -Math
                    -console.log(Math.abs(-5));
                    -console.log(Math.ceil(4.2));
                    -console.log(Math.floor(4.7));
                    -console.log(Math.round(4.5));
                    -console.log(Math.max(1,3,5,7,9));
                    -console.log(Math.min(1,3,5,7,9));
                    -console.log(Math.random());
                    -console.log(Math.sqrt(16));
                    , Array, String methods
    -Callback and higher order function
        -higher order function: map, filter, foreach, reduce, callback functions
## Javascript part-5(intermediate)        
    -ES6,ES7 
        -var let vs const
        -Module
        -spread Operator, Rest Perameters
        -Destructure

## JavaScript part -6  (Intermediate)      
        -higher order function:foreach(), map(),filter(), reduce(),every(), sort(), callback functions, synchronous vs asynchronous
        
## JaavaScript part-7 (Intermediate)       
        -Error Handling
        -Asynchronous programming: async, await , then , catch, callback
        -JSON
        -API calling

## javaScript part-8 (Intermediate)
        -local Storage
        -DOM and Event Handler
    
## Javascript part -9 (Intermediate)    
        -OOP
        -A simple bank app

## Javascript part-10(Advanced)
        -Testing
## Javascript part-11 (Advanced)
        -Project:Ecommerce front-page
